<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.orderapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OrderApp">
        <activity android:name=".ProvidersMyOrdersActivity"
            android:label="@string/my_orders">

        </activity>
        <activity android:name=".ProviderOrderDetailsActivity"
            android:label="Provider Order Details">

        </activity>
        <activity android:name=".ProviderMainActivity"
            android:label="Providers">

        </activity>
        <activity android:name=".UserOrderDetailsActivity" />
        <activity android:name=".UserAddOrderActivity"
            android:label="New Order" />
        <activity android:name=".ProfileActivity"
            android:label="Profile" />
        <activity android:name=".PhoneSignUpActivity" />
        <activity android:name=".RegisterActivity"
            android:label="Register" />
        <activity android:name=".UserMainActivity"
            android:label="Home" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>